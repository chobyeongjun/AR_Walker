# Step 2: AR Walker 전원 시스템 이해

> 우리 프로젝트의 전기가 어디서 오고, 어디로 가는지 이해하자.

---

## 1. 전원 트리 (Power Tree)

AR Walker의 전원은 **48V 배터리 하나**에서 시작해서 3단계로 나뉜다:

```
[48V 리튬 배터리]
    │
    ├──→ [Elmo Gold Twitter] → [모터] (48V 직접 사용)
    │
    ├──→ [벅 컨버터 (Buck Converter)]
    │         │
    │         └──→ [5V 출력]
    │                 │
    │                 ├──→ [Jetson Orin Nano] (5V, 최대 3A)
    │                 │
    │                 └──→ [LDO (Low Dropout Regulator)]
    │                          │
    │                          └──→ [3.3V 출력]
    │                                  │
    │                                  ├──→ [STM32H743]
    │                                  ├──→ [ESP32-S3]
    │                                  ├──→ [INA828 x4]
    │                                  └──→ [CAN 트랜시버]
    │
    └──→ [EBIMU RF 리시버] (자체 전원 또는 5V/3.3V)
```

---

## 2. 벅 컨버터 (Buck Converter) — 48V → 5V

### 뭐하는 놈인가?
- 높은 전압을 **낮은 전압으로 변환**하는 장치
- "스위칭" 방식이라 **효율이 높음** (보통 85~95%)

### 왜 필요한가?
- STM32, ESP32는 3.3V에서 동작
- 48V를 직접 넣으면 **즉시 파괴**됨
- 48V → 3.3V를 한 번에 하면 효율이 떨어짐
- 그래서 **48V → 5V → 3.3V** 두 단계로 변환

### 동작 원리 (간단 버전)
```
48V ──[스위치]──[인덕터]──[캐패시터]── 5V 출력
        │                      │
        └── 제어 IC가 스위치를   │
            빠르게 ON/OFF하여    │
            평균 전압을 5V로 맞춤 │
```

1. 스위치 ON → 인덕터에 에너지 저장
2. 스위치 OFF → 인덕터가 저장된 에너지를 출력으로 방출
3. 이 과정을 수백 kHz로 반복
4. 캐패시터가 출력을 매끄럽게 유지

### 핵심 스펙 선정 시 고려사항
```
입력 전압 범위: 42V ~ 54V (48V 리튬 배터리 충방전 범위)
출력 전압: 5V
출력 전류: 최소 5A (Jetson 3A + MCU + 여유)
효율: 90% 이상 목표
```

---

## 3. LDO (Low Dropout Regulator) — 5V → 3.3V

### 뭐하는 놈인가?
- 전압을 **조금만** 낮추는 간단한 레귤레이터
- 벅 컨버터보다 **노이즈가 적음** → 민감한 IC에 적합

### 왜 벅 컨버터 대신 LDO?
```
벅 컨버터: 효율 높음, but 스위칭 노이즈 있음
LDO:       효율 낮음, but 깨끗한 전원 출력

5V → 3.3V는 차이가 1.7V밖에 안 됨 → LDO 효율도 괜찮음
효율 = 3.3V / 5V = 66% (이 경우는 OK)
```

### 핵심 스펙
```
입력: 5V
출력: 3.3V
전류: 최소 1A (STM32 300mA + ESP32 300mA + 센서 + 여유)
드롭아웃 전압: < 0.5V (5V - 3.3V = 1.7V이므로 충분)
```

---

## 4. 디커플링 캐패시터 (Decoupling Capacitor)

### 이것이 왜 모든 IC 옆에 있는가?

IC(칩)가 갑자기 전류를 많이 필요로 하면:
```
전원 라인이 멀면 → 순간적으로 전압이 떨어짐 (전압 강하)
                 → IC가 오동작 할 수 있음
```

디커플링 캡 = **IC 옆에 놓는 작은 배터리 역할**
```
IC가 갑자기 전류 필요 → 바로 옆 캐패시터에서 즉시 공급
                      → 전압 강하 방지
```

### 규칙
- **모든 IC의 VCC-GND 핀 옆에** 100nF (0.1μF) 세라믹 캐패시터 배치
- 가능하면 **가까이** (짧은 배선으로)
- 전원 입력부에는 큰 캡도 추가 (10μF ~ 100μF)

```
[3.3V] ──┬── [100nF] ── [GND]    ← IC 바로 옆
         │
         └── [STM32 VCC 핀]
```

---

## 5. 접지 (GND, Ground)

### 왜 중요한가?
- GND = 전기 회로의 **기준점** (0V)
- 모든 전압은 GND 기준으로 측정됨
- **모든 부품의 GND는 하나로 연결**되어야 함

### AR Walker에서의 GND
```
48V 배터리 GND ─┬─ Elmo GND
                ├─ 벅 컨버터 GND
                ├─ STM32 GND
                ├─ ESP32 GND
                ├─ Jetson GND
                └─ 센서 GND
```

### 주의사항
- 전력용 GND (모터)와 신호용 GND (MCU)는 **PCB에서 분리 후 한 점에서 연결** (Star Ground)
- 그래야 모터의 큰 전류가 MCU의 섬세한 신호에 노이즈를 주지 않음

---

## 6. 전력 예산 (Power Budget) 계산

AR Walker 전체 시스템의 전력을 계산해보자:

### 48V 레일
| 부품 | 전압 | 전류 (최대) | 전력 |
|------|------|------------|------|
| Elmo + 모터 (x2~4) | 48V | 10~20A | 480~960W |
| 벅 컨버터 입력 | 48V | ~1A | ~48W |
| **합계** | | | **~530~1010W** |

### 5V 레일 (벅 컨버터 출력)
| 부품 | 전압 | 전류 (최대) | 전력 |
|------|------|------------|------|
| Jetson Orin Nano | 5V | 3A | 15W |
| LDO 입력 | 5V | 1A | 5W |
| **합계** | | | **~20W** |

### 3.3V 레일 (LDO 출력)
| 부품 | 전압 | 전류 (최대) | 전력 |
|------|------|------------|------|
| STM32H743 | 3.3V | 300mA | 1W |
| ESP32-S3 | 3.3V | 300mA | 1W |
| INA828 x4 | 3.3V | 20mA | 0.07W |
| CAN 트랜시버 | 3.3V | 50mA | 0.17W |
| **합계** | | | **~2.3W** |

---

## 연습 문제

### 문제 1: 벅 컨버터 효율
> 벅 컨버터의 효율이 90%이고, 5V에서 5A를 출력해야 한다.
> 48V 입력에서 몇 A의 전류가 필요한가?

<details>
<summary>정답 보기</summary>

```
출력 전력 = 5V × 5A = 25W
입력 전력 = 25W / 0.90 = 27.8W (효율 90%)
입력 전류 = 27.8W / 48V = 0.58A
```
</details>

### 문제 2: 배터리 수명
> 48V 20Ah 배터리를 사용한다.
> 시스템이 평균 500W를 사용하면 몇 시간 쓸 수 있나?

<details>
<summary>정답 보기</summary>

```
배터리 에너지 = 48V × 20Ah = 960Wh
사용 시간 = 960Wh / 500W = 1.92시간 ≈ 약 2시간
```
</details>

### 문제 3: LDO 발열
> LDO가 5V → 3.3V로 변환하고, 출력 전류가 1A일 때
> LDO에서 발생하는 열은 몇 W인가?

<details>
<summary>정답 보기</summary>

```
열 = (입력전압 - 출력전압) × 전류
   = (5V - 3.3V) × 1A
   = 1.7W

→ 1.7W면 방열이 필요할 수 있음 (손으로 만지면 뜨거움)
```
</details>

---

## 핵심 요약

| 변환 단계 | 방식 | 입력 | 출력 | 대상 |
|-----------|------|------|------|------|
| 1단계 | 없음 (직접) | 48V | 48V | Elmo 모터 드라이버 |
| 2단계 | 벅 컨버터 | 48V | 5V | Jetson, LDO 입력 |
| 3단계 | LDO | 5V | 3.3V | STM32, ESP32, 센서 |

**기억할 것:**
- 모든 IC 옆에 **디커플링 캡** (100nF)
- 전력용 GND와 신호용 GND **분리 후 한 점 연결**
- 전력 예산을 미리 계산해서 부품 선정

---

## 다음 단계
→ [Step 3: 핵심 부품 역할 정리](step03_components.md)
